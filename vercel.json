{
  "builds": [
    {
      "src": "api/app.py",
      "use": "@vercel/python",
      "config": { "includeFiles": "api/templates/**" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "api/app.py" }
  ]
}
